const user={
    name:'manish',
    id:123,
    company:'abs'
}
console.log(user.id);

// destructuring of object
const {id}= user
console.log(id)

// Json = javascript object notation.
// json exists in array or object format.  